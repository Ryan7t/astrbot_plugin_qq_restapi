"""Automated tests for the qq_restapi plugin.

Tests are intentionally kept independent from a running AstrBot instance.  Run
them with the project's virtualenv so AstrBot's runtime dependencies are
available, for example:

    venv\\Scripts\\python.exe -m unittest discover -s tests -v
"""
